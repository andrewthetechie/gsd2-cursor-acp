---
phase: 06-fix-readme-for-standalone-distribution-remove-gsd-npm-install-directions-add-git-based-install-instructions-replace-code-centric-usage-section-with-gsd2-configuration-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - README-01
must_haves:
  truths:
    - "Installation section uses gsd install github:OWNER/REPO, not npm install"
    - "No reference to @gsd npm org install or npm workspace installs anywhere in Installation"
    - "Usage section explains gsd2 extension auto-registration and /model selection, not raw TypeScript streaming code"
    - "Configuration, Error Handling, Permission Policy, and Advanced sections are preserved unchanged"
  artifacts:
    - path: "README.md"
      provides: "Standalone distribution documentation with gsd2 extension install flow"
      contains: "gsd install github:"
  key_links:
    - from: "README.md Installation section"
      to: "gsd2 extension install command"
      via: "gsd install github:OWNER/REPO"
      pattern: "gsd install github:"
---

<objective>
Rewrite README.md Installation and Usage sections to reflect that this package is a gsd2 extension installed via `gsd install`, NOT an npm package. The extension auto-registers its provider — users do not call `registerCursorAcpProvider()` manually. After install, Cursor models appear in gsd2's `/model` selection automatically.

Purpose: A developer can follow the README to install the extension and start using Cursor models in gsd2 without hitting npm 404 errors or writing boilerplate registration code.

Output: README.md with corrected Installation and Usage sections; all other sections preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Installation and Usage sections for gsd2 extension workflow</name>
  <files>README.md</files>

  <read_first>
    - README.md — read the entire file; note exact section boundaries
    - package.json — confirm package name and metadata
  </read_first>

  <action>
Edit README.md with the following replacements. Do NOT touch any section outside Installation and Usage.

**1. Replace the Installation section (lines 12-21)**

Remove the entire current Installation section and replace with exactly:

```
## Installation

Install as a gsd2 extension directly from the git repository:

```bash
gsd install github:OWNER/REPO
```

Replace `OWNER/REPO` with the actual GitHub path (e.g. `my-org/gsd-cursor`).

The extension registers itself automatically — no manual configuration needed.
```

**2. Replace the Usage section (lines 30-45)**

Remove the entire current Usage section and replace with exactly:

```
## Usage

Once installed, the Cursor ACP provider is available in gsd2 automatically.

### 1. Verify the extension is loaded

After installing, the provider auto-registers via the gsd2 Extension API. Cursor models will appear in gsd2's model list. You can verify with:

```bash
gsd /model
```

Look for models prefixed with `cursor-acp/` (e.g. `cursor-acp/claude-3-5-sonnet`).

### 2. Select a Cursor model

Choose a Cursor model via the `/model` command in a gsd2 session, or set it in your gsd2 configuration:

```yaml
# ~/.gsd/agent/config.yaml (syntax depends on your gsd2 version)
model: cursor-acp/claude-3-5-sonnet
```

### 3. Verify the integration

Run gsd2 normally. On first use the extension spawns `cursor-agent` and establishes an ACP session. Check gsd2 logs for a `cursor-acp` provider line at startup.
```

**Rules:**
- Preserve exact content of all other sections: Prerequisites, Configuration, Error Handling, Permission Policy, Advanced
- Do not reorder sections
- The file must still begin with `# @gsd/pi-ai-cursor-acp` and the tagline
- The Error Handling section still references `registerCursorAcpProvider` and import paths — that is fine, those are API reference examples, not user setup instructions
  </action>

  <verify>
    <automated>
      grep -c "gsd install github:" README.md | grep -q "1" && echo "PASS: gsd install present" || echo "FAIL: gsd install missing"
      grep -c "npm install @gsd/pi-ai-cursor-acp" README.md | grep -q "^0$" && echo "PASS: npm org install removed" || echo "FAIL: npm org install still present"
      grep -c "npm install.*--workspace" README.md | grep -q "^0$" && echo "PASS: workspace install removed" || echo "FAIL: workspace install still present"
      grep -c "for await" README.md | grep -q "^0$" && echo "PASS: streaming loop removed" || echo "FAIL: streaming loop still present"
      grep -c "gsd /model" README.md | grep -q "[1-9]" && echo "PASS: /model command shown" || echo "FAIL: /model command missing"
      grep -c "cursor-acp/" README.md | grep -q "[1-9]" && echo "PASS: model namespace shown" || echo "FAIL: model namespace missing"
      grep -q "## Configuration" README.md && echo "PASS: Configuration preserved"
      grep -q "## Error Handling" README.md && echo "PASS: Error Handling preserved"
      grep -q "## Permission Policy" README.md && echo "PASS: Permission Policy preserved"
      grep -q "## Advanced" README.md && echo "PASS: Advanced preserved"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "npm install @gsd/pi-ai-cursor-acp" README.md` returns nothing (npm org install gone)
    - `grep "npm install.*--workspace" README.md` returns nothing (workspace install gone)
    - `grep "gsd install github:" README.md` returns at least one match (extension install present)
    - `grep "for await" README.md` returns 0 lines (streaming loop removed from Usage)
    - `grep "gsd /model" README.md` returns at least one match (model selection command shown)
    - `grep "cursor-acp/" README.md` returns at least one match (model namespace reference)
    - `grep "auto-registers" README.md` returns at least one match (auto-registration explained)
    - `grep "## Configuration" README.md` returns a match (section preserved)
    - `grep "## Error Handling" README.md` returns a match (section preserved)
    - `grep "## Permission Policy" README.md` returns a match (section preserved)
    - `grep "## Advanced" README.md` returns a match (section preserved)
  </acceptance_criteria>

  <done>README.md Installation section uses `gsd install github:OWNER/REPO`; Usage section explains auto-registration and /model selection; no npm org install references remain; all other sections byte-for-byte identical to original.</done>
</task>

</tasks>

<verification>
After task completion, verify:

1. `grep "npm install @gsd/pi-ai-cursor-acp" README.md` — no output (npm org install removed)
2. `grep "gsd install github:" README.md` — at least one line (extension install present)
3. `grep "for await" README.md` — 0 lines (raw streaming loop gone)
4. `grep "gsd /model" README.md` — at least one line (model selection shown)
5. `grep "## Configuration\|## Error Handling\|## Permission Policy\|## Advanced" README.md` — 4 lines (all preserved sections intact)
</verification>

<success_criteria>
A developer reading README.md can:
1. Install the extension via `gsd install github:OWNER/REPO` without hitting npm 404
2. Understand the provider auto-registers — no manual code needed
3. Find and select Cursor models via gsd2's /model command
4. Find unchanged Configuration, Error Handling, Permission Policy, and Advanced sections
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-readme-for-standalone-distribution-remove-gsd-npm-install-directions-add-git-based-install-instructions-replace-code-centric-usage-section-with-gsd2-configuration-guide/06-01-SUMMARY.md`
</output>
